#include <stdio.h>

struct Employee
{
    char firstname[20];
    char name[15];
    char lastname[15];
    char dolzhnost[15];
    char gender[10];
    int start_work[10];
};

int main(void)
{
    struct Emplyoee start_work;
    int sum = 0;
    int sredn = 0;

    for (int i = 0; i < start_word; ++i)
        {
            sum += start_word[i];
            sredn = sum / start_word;
        }

    for (int j = 0; j < sredn; ++i){

        printf("%c, %c, %c, %c, %c, %d", Employee.firstname[j], Employee.name[j], Employee.lastname[j], Employee.dolzhnost[j], Employee.genre[j], Employee.start_work[j]);
}
